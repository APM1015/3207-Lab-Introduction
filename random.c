#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "random.h"

int main()
{
	
	int c = rand() % 26;
	char ch = (char)(char+65);
	putf('%c',ch);
	return 0;
	
	/*
  String chars[26] = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
	
  int a;
  string letter = "";
		
/* seed the randomizer */
	/*
	srand( (unsigned)time(NULL) );

	for(a=0;a<27;a++)
		putf('%c',a);

	return(0);
	*/
	
	
}
